import React from 'react';
import styled from 'styled-components';

const ServiceDropDown = () => {
  return <Container>ServiceDropDown</Container>;
};

export default ServiceDropDown;

const Container = styled.div`
  padding: ${(props) => props};
`;
