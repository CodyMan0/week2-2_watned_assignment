import React from 'react';
import styled from 'styled-components';

const ContentPage = () => {
  return <Container>ContentPage</Container>;
};

export default ContentPage;

const Container = styled.div`
  flex: 3.5;
`;
